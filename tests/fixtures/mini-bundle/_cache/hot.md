# Hot

Semantic snapshot of the current bundle state. The validator is the deterministic
conformance gate; reserved files (`index.md`, `log.md`) and out-of-scope zones
(`_`-prefixed paths, dot-files) are handled by explicit rules, so this snapshot
stays free-form.
