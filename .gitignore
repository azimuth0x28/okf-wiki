.env
.DS_Store
*.pyc
__pycache__/
.worktrees/

# Skill creator benchmark/review workspaces and local packages
.skills/*-workspace/
.skills/packages/

# Python build artifacts (pyproject.toml / `python -m build`)
/dist/
/build/
*.egg-info/

# Local bundle — personal knowledge, never published with the framework
/vault/

# Packed browser extension (rebuildable from extensions/brain-capture/)
extensions/brain-capture/*.zip

# Agent skill symlinks (created by setup.sh) — these are just pointers to .skills/
# We track them so cloners get the symlinks too, but if your git doesn't
# preserve symlinks, run `bash setup.sh` after cloning.

# okf-wiki working directories
.tmp/
.omo/
.codegraph/
.logs/

# Python venv + generated version
.venv/
okf_wiki/_version.py
